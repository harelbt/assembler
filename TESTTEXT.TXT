	
.data 9-,8
longlabelprobablytoomuchihopethatitwillcauseanerror: add r1 r2
longlabelprobablytoomuchihopetha: add r1 r2
.string "al:"
8labe: add r1 r2
lab : .data 7,8
.data 7,5,
.data ,7,8
.data 7 8
.data 7 , 	, 8
.data 
.data a, 7
.data .string 7,4
.string"a"
.stringa"3"
.string "label:"
.string:
aj .string
aj.string
.extern jkj nk
add .data r2 r1
.string 7,3,2
.data +7,-0, -3
r0: .data 0,1,2
add: r1 r2
.data: add r1 r2
add add r1 r2
LABEL1:	mov	#7	r1	
	mov	# 7	r1	 
;שגיאה(רווח)
mov str stri
				
LABEL2:	mov	#-9	r2	
	mov	#-9	r 2	 
;שגיאה(רווח)
				
	mov	r3	r2	
				
	mov	#-9	#8	 
;שגיאה(אוגר יעד)
LABEL 3:	mov	#-9	# 8	 
;שגיאה(רווח, אוגר יעד)
".string"
.skak
"".string
.string"	
sti: a .string	
a .string
.s .string "afs"
stt: a .data 7,   6	
a .data 7,   6	
stt: a .entry stt	
a .extern stt
hello: hello2: .data 8
: .data 9	
LABEL 3	mov	# -9	# 8	 
;שגיאה(רווח, אוגר יעד,תווית ללא ;)
	mov	r1	r2	
	mov	r1		 
;שגיאה(אופרנד אחד)
	mov	#9		 
;שגיאה(אופרנד אחד)
	mov	r9	r2	 
;שגיאה(אופרנד מקור לא תקף)
	mov	r1	r8	 
;שגיאה(אופרנד יעד לא תקף)
	mov	r1		 
;שגיאה(אופרנד חסר)
	mov	nexist	r2	 
;שגיאה(התווית באופרנד המקור לא קיימת)
 d;comment		
		
cmp	#-9;	r2		
	cmp	#-9	r2	
	cmp	#-9	r 2	 
;שגיאה(רווח)
	cmp	#-9	X	
				
	cmp	#-9	#8	
	cmp	#-9	# 8	 
;שגיאה(רווח)
				
	cmp	# -9	#8	 
;שגיאה(רווח)
	cmp	#9	#8	
	cmp	r1	r2	
	cmp	r1	#7	
	cmp	#9	r2	
	cmp	r1		 
;שגיאה(אופרנד אחד)
	cmp	#9		 
;שגיאה(אופרנד אחד)
	cmp	r9	r2	 
;שגיאה(אופרנד מקור לא תקף)
	cmp	r1	r8	 
;שגיאה(אופרנד יעד לא תקף)
	cmp	r1		 
;שגיאה(אופרנד חסר)
				
	add	#7	r1	
	add	LABEL1	r1	
	add	# 7	r1	 
;שגיאה(רווח)
				
	add	#-9	r2	
	add	#-9	r 2	 
;שגיאה(רווח)
				
X:	add	r3	r2	
				
	add	#-9	#8	 
;שגיאה(אוגר יעד)
	add	#-9	# 8	 
;שגיאה(רווח, אוגר יעד)
				
	add	# -9	# 8	 
;שגיאה(רווח, אוגר יעד,תווית ללא ;)
	add	r1	r2	
	add	r1		 
;שגיאה(אופרנד אחד)
	add	#9		 
;שגיאה(אופרנד אחד)
	add	r9	r2	 
;שגיאה(אופרנד מקור לא תקף)
	add	r1	r8	 
;שגיאה(אופרנד יעד לא תקף)
	add	r1		 
;שגיאה(אופרנד חסר)
	add	#5	abc	
				
	sub	#7	r1	
	sub	# 7	r1	 
;שגיאה(רווח)
				
	sub	#-9	r2	
	sub	#-9	r 2	 
;שגיאה(רווח)
				
	sub	r3	r2	
				
	sub	#-9	#8	 
;שגיאה(אוגר יעד)
	sub	#-9	# 8	 
;שגיאה(רווח, אוגר יעד)
				
	sub	# -9	# 8	 
;שגיאה(רווח, אוגר יעד,תווית ללא ;)
	sub	r1	r2	
	sub	r1		 
;שגיאה(אופרנד אחד)
	sub	#9		 
;שגיאה(אופרנד אחד)
	sub	r9	r2	 
;שגיאה(אופרנד מקור לא תקף)
	sub	r1	r8	 
;שגיאה(אופרנד יעד לא תקף)
	sub	r1		 
;שגיאה(אופרנד חסר)
				
	lea	#7	r1	 
;שגיאה(אין כתובת מקור)
	lea	# 7	r1	 
;שגיאה(רווח)
				
	lea	#-9	r2	 
;שגיאה(אין כתובת מקור)
	lea	#-9	r 2	 
;שגיאה(רווח, אין כתובת מקור)
				
	lea	r3	r2	
				
	lea	#-9	#8	 
;שגיאה(אוגר יעד, אין כתובת מקור)
	lea	#-9	# 8	 
;שגיאה(רווח, אוגר יעדת אין כתובת מקור)
				
	lea	# -9	# 8	 
;שגיאה(רווח, אוגר יעד,תווית ללא ;)
	lea	r1	r2	
	lea	r1		 
;שגיאה(אופרנד אחד)
	lea	#9		 
;שגיאה(אופרנד אחד)
	lea	DATA	X	 
;שגיאה( אין אוגר מקור)
	lea	r5	X	
	lea	r9	r2	 
;שגיאה(אופרנד מקור לא תקף)
lea	r3	r2 r4

	lea	r1	r8	 
;שגיאה(אופרנד יעד לא תקף)
	lea	r1		 
;שגיאה(אופרנד חסר)
				
	clr	DATA		
	clr	X		
	clr	r1		
	clr	r9	 
;שגיאה(אופרנד לא תקף)	
	clr	#7	 
;שגיאה(אופרנד לא חוקי)	
	clr	#-7	 
;שגיאה(אופרנד לא חוקי)	
	clr	# 7	 
;שגיאה(רווח)	
	clr	r 9	 
;שגיאה(רווח)	
	clr	LABEL3		
	clr	r2	r1	 
;שגיאה(אופרנד עודף)
				
	not	DATA		
	not	X		
	not	r1		
	not	r9	 
;שגיאה(אופרנד לא תקף)	
	not	#7	 
;שגיאה(אופרנד לא חוקי)	
	not	#-7	 
;שגיאה(אופרנד לא חוקי)	
	not	# 7	 
;שגיאה(רווח)	
	not	r 9	 
;שגיאה(רווח)	
	not	LABEL3		
	not	r2	r1	 
;שגיאה(אופרנד עודף)
				
	inc	DATA		
	inc	X		
	inc	r1		
	inc	r9	 
;שגיאה(אופרנד לא תקף)	
	inc	#7	 
;שגיאה(אופרנד לא חוקי)	
	inc	#-7	 
;שגיאה(אופרנד לא חוקי)	
	inc	# 7	 
;שגיאה(רווח)	
	inc	r 9	 
;שגיאה(רווח)	
	inc	LABEL3		
	inc	r2	r1	 
;שגיאה(אופרנד עודף)
				
	dec	DATA		
	dec	X		
	dec	r1		
	dec	r9	 
;שגיאה(אופרנד לא תקף)	
	dec	#7	 
;שגיאה(אופרנד לא חוקי)	
	dec	#-7	 
;שגיאה(אופרנד לא חוקי)	
	dec	# 7	 
;שגיאה(רווח)	
	dec	r 9	 
;שגיאה(רווח)	
	dec	LABEL3		
	dec	r2	r1	 
;שגיאה(אופרנד עודף)
	dec	&LABEL2		 
;שגיאה(אופרנד לא תקף)
				
	jmp	DATA		
	jmp	X		
	jmp	r1	 
;שגיאה(אופרנד לא תקף)	
	jmp	r9	 
;שגיאה(אופרנד לא תקף)	
	jmp	#7	 
;שגיאה(אופרנד לא חוקי)	
	jmp	#-7	 
;שגיאה(אופרנד לא חוקי)	
	jmp	# 7	 
;שגיאה(רווח)	
	jmp	r 9	 
;שגיאה(רווח)	
	jmp	LABEL3		
	jmp	r2	r1	 
;שגיאה(אופרנד עודף)
	jmp	&LABEL2		
	jmp	&X	 
;שגיאה(לא משפט הוראה במיעון יחסי)	
				
	bne	DATA		
	bne	X		
	bne	r1	 
;שגיאה(אופרנד לא תקף)	
	bne	r9	 
;שגיאה(אופרנד לא תקף)	
	bne	#7	 
;שגיאה(אופרנד לא חוקי)	
	bne	#-7	 
;שגיאה(אופרנד לא חוקי)	
	bne	# 7	 
;שגיאה(רווח)	
	bne	r 9	 
;שגיאה(רווח)	
	bne	LABEL3		
	bne	r2	r1	 
;שגיאה(אופרנד עודף)
	bne	&LABEL2		
	bne	&X	 
;שגיאה(לא משפט הוראה במיעון יחסי)	
				
	jsr	DATA		
	jsr	X		
	jsr	r1	 
;שגיאה(אופרנד לא תקף)	
	jsr	r9	 
;שגיאה(אופרנד לא תקף)	
	jsr	#7	 
;שגיאה(אופרנד לא חוקי)	
.data 6,  7, -7
.data #6,  7, -7
;שגיאה(יש #)
.string "khskhbkd"
.string "jdcbjcn""
.string "jdcbjcn""s
.string "dcncdnlc"
.string "
.data 6,  7, -7
.data 9,
.data -3
kdjndkjcnjknnjkncln
;שגיאה(לא אומר כלום)
	jsr	#-7
LLL: kka #6 r1
;שגיאה(אופרנד לא חוקי)	
	jsr	# 7	 
;שגיאה(רווח)	
	jsr	r 9	 
;שגיאה(רווח)	
	jsr	LABEL3		
	jsr	r2	r1	 
;שגיאה(אופרנד עודף)
	jsr	&LABEL2		
	jsr	&X	 
;שגיאה(לא משפט הוראה במיעון יחסי)	
				
	red	DATA		
	red	X		
	red	r1		
	red	r9	 
;שגיאה(אופרנד לא תקף)	
	red	#7	 
;שגיאה(אופרנד לא חוקי)	
	red	#-7	 
;שגיאה(אופרנד לא חוקי)	
	red	# 7	 
;שגיאה(רווח)	
	red	r 9	 
;שגיאה(רווח)	
	red	LABEL3		
	red	r2	r1	 
;שגיאה(אופרנד עודף)
				
				
	prn	r2		
	prn	r 2	 
;שגיאה(רווח)	
	prn	X		
	prn			
	prn	#8		
	prn	# 8	 
;שגיאה(רווח)	
	prn			
	prn	#8		
	prn	r2		
	prn	#7		
	prn	r2		
	prn	r8	 
;שגיאה(אופרנד יעד לא תקף)	
	prn		 
;שגיאה(אופרנד חסר)	
	prn	str		
				
	hme	 
;שגיאה(הוראה לא קיימת)		
LABEL1:	prn	str	 
;שגיאה(הגדרה חוזרת של תווית)	
				
	rts			
	stop			
DATA:	.data	-6		
stri: .string "certy"
str:	.string	"abcdef"		
JH:	.entry	str	
;להתעלם מהתווית	
	.extern	abc		
###